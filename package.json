{
  "name": "proxo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@proxo/api": "workspace:*",
    "@proxo/core": "workspace:*",
    "@proxo/orchestrator": "workspace:*",
    "@proxo/wallet": "workspace:*",
    "@types/node": "^25.3.0",
    "typescript": "^5.7.0",
    "viem": "^2.46.2",
    "vitest": "^3.0.0"
  }
}
